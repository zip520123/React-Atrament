{
  "name": "canvas-web",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "atrament": "^0.2.4",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4",
    "throttle-debounce": "^2.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
      "deploy": "gh-pages -d build"
  },
  "homepage": "https://zip520123.github.io/React-Atrament",
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
